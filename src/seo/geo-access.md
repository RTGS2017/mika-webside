# GEO 爬虫可访问性说明

`robots.txt` 对 `User-agent: *` 与显式的 `User-agent: OAI-SearchBot` 均写明 `Allow: /`，允许相关爬虫抓取站点正文。

这是**可访问性**配置：保证爬虫不被 robots 规则阻断。

这**不是**：

- ChatGPT / AI 产品展示或推荐保证
- 答案引用或引用位保证
- 搜索结果位置保证

页面与结构化数据中的 GEO 表述应与内容合同一致：衡量信息是否可理解、可支撑、可引用；不承诺任何 AI 产品的收录、推荐或引用。
